dockerhub:
  username: dockerhub username
  password: dockerhub password
webhook_token: webhook token triggered by github

test:
  registry: private registry used for tests
  credentials_good: valid npm token for the private registry
  credentials_bad: invalid token
