docker:
  username: dockerhub username
  password: dockerhub password
  webhook_token: webhook token triggered by the dockerhub master build

test:
  registry: private registry used for tests
  credentials:
    good: valid npm token for the private registry
    bad: invalid token
